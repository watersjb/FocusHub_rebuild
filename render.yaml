services:
  - type: web
    name: focushub
    env: static
    buildCommand: echo "No build needed - static files"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
